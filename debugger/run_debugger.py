import sys
import os
import subprocess

# This runs an independent debugger UI that has the same functions as the main 
# app, but is used for testing and debugging purposes.
def main():
  print('Starting debugger UI...')

if __name__ == "__main__": 
  main()
